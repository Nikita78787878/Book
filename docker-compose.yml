services:
  postgres:
    image: postgres:16
    container_name: book-postgres
    environment:
      POSTGRES_DB: bookdb
      POSTGRES_USER: book
      POSTGRES_PASSWORD: book
    ports:
    - '5430:5432'